# BrowserSelector 開発計画（簡素版）

最終更新: 2025-09-02

## 1. 概要
本計画は、BrowserSelector（WPF + MVVM）の現状と今後の作業を「不具合対応」「今後の実装計画」に分けて明確化します。完了済みの内容は簡潔に記載します。

## 2. 実装済み（簡素）
- アーキテクチャ/基盤: DI、MVVM、設定の保存/読み込み、国際化（日/英）
- 主要画面: メイン画面、設定画面（タブ構成）、ログビューア
- コア機能: ブラウザ検出と起動、URL正規化/短縮URL展開、システムトレイ
- 視覚/アクセシビリティ: 透明化ウィンドウ、角丸、キーボードナビゲーション
- ログ: ファイル/コンソール出力、レベル切替、クリーニング
- テスト: 単体/統合/一部UI/E2E を実行可能

## 3. 不具合対応（詳細計画）

### 3.1 設定/一般タブ
- ログ設定が残置 → 削除（ログ設定タブへ統合済み）

対応: UIから当該ブロック削除済み（コミット済）。

### 3.2 設定/表示タブ
- 透明化チェックが保存・適用されない
- 角の丸みは保存されるが適用されない
- カスタム背景色が選択できない

対応項目（実装手順）:
1) `VisualSettings` と `TransparentWindow` の双方向反映を修正（Opacity/IsTransparent/CornerRadius）
2) プロパティ変更時の適用ハンドラを `SettingsViewModel` 経由で通知
3) 背景色選択: カラーダイアログ導入、`VisualSettings.BackgroundColor` へバインド、保存/読込/即時反映
4) 単体/UIテスト追加（保存→再起動→反映、即時反映）

完了条件: 保存/再起動/即時反映が一致、UI上で視覚確認できること。

### 3.3 設定/ブラウザタブ
- プロトコル設定が残存 → 削除対象（別機能）
- ブラウザの追加/削除/編集が動作しない

対応項目（実装手順）:
1) UIからプロトコル設定を削除（コミット済み）
2) 追加/編集/削除コマンドを `IBrowserService` と `SettingsViewModel` に実装
3) ファイル選択/アイコン選択ダイアログの導入（パス検証/重複名チェック）
4) 保存/読込/一覧リフレッシュの一貫性を担保
5) 単体/UIテスト追加

完了条件: CRUDがUIから完結し、再起動後も反映される。

### 3.4 設定/URLルールタブ
- サンプル表示のみで、編集/追加が動作しない
- 「ルールを更新」の意味が不明

対応項目（実装手順）:
1) ルール追加/編集ダイアログ実装、`IUrlRuleService` 統合
2) 「ルールを更新」を「並び順を保存」に改称、並び替えの保存機能実装
3) ルール適用タイミングと保存/読込の設計を統一
4) 単体/UIテスト追加

完了条件: 追加/編集/並び替え保存が動作し、用語が明確。

### 3.5 設定/アクセシビリティタブ
- 現状利用頻度が低いため全般不要 → 当面非表示

対応: `Visibility=Collapsed` で非表示（コミット済み）。再開時に表示復帰可能。

### 3.6 ログ設定タブ
- 選択されているフォルダ名/パスが不明

対応項目（実装手順）:
1) 現在の出力先フルパスをテキスト表示（選択直後に即時更新）
2) ログレベル表記は短縮名（INFO/DEBUG等）で統一
3) 単体/UIテスト追加

完了条件: UIで出力先パスを明確に確認できる。

## 4. 今後の実装計画（詳細）

優先度高（1〜2週）
- 表示タブの保存/適用不具合の解消（3.2）
- ブラウザタブのCRUD復旧（3.3）

中期（2〜3週）
- URLルールタブの操作性向上と機能実装（3.4）
- ログ設定タブのパス可視化（3.6）

継続タスク
- UI/用語の一貫性向上、アクセシビリティ項目の再評価、テスト強化

## 5. スケジュール
- Week 1: 3.2 表示タブ修正 完了、最小テスト追加
- Week 2: 3.3 ブラウザCRUD 完了、テスト追加
- Week 3: 3.4 URLルール操作/用語整理 完了
- Week 4: 3.6 ログパス表示 完了、回帰テスト

## 6. 受け入れ基準（共通）
- 設定の保存/再起動/即時反映が一致
- 主要操作はINFOログで開始/完了/結果を記録（短縮レベル名）
- 単体/UIテストが追加されCIで成功

## 7. リスクと対策
- 既存設定との互換性: 破壊的変更を避け、マイグレーションが必要な場合は自動変換
- UIの混乱: 用語統一と簡潔化（特にURLルール）
- 過剰ログ: 既定DEBUGだが、不要冗長は抑制し重要イベントはINFOで記録

## 8. 変更履歴
- 2025-09-02: 文書を簡素化。不具合対応/今後の計画を詳細化。設定タブから不要項目を削除（一般/プロトコル）、アクセシビリティ非表示を反映。


