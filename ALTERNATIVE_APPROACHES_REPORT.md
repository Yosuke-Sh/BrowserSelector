# 代替アプローチ検討レポート

## 概要
カバレッジ計測に含まれない問題を解決するため、複数の代替アプローチを検討・実装しました。すべてのアプローチで同じ問題が発生し、根本的な解決には至りませんでした。

## 実施した代替アプローチ

### 1. dotnet-coverageツールの検討と実装 ❌
**実施内容:**
- dotnet-coverageツールのインストール確認
- コマンド構文の検討

**結果:**
- ツールは既にインストール済み
- コマンド構文の問題で実行できず
- ヘルプが表示されない問題

**問題点:**
- コマンド構文が不明確
- ツールの動作が不安定

### 2. プロジェクト個別でのカバレッジ測定 ❌
**実施内容:**
- Coreプロジェクトのテストのみを実行
- フィルターを使用した特定テストの実行

**結果:**
- テストは正常に実行される
- カバレッジファイルは生成される
- しかし、`BrowserSelector.App`のみが含まれる

**問題点:**
- プロジェクト個別でも同じ問題が発生
- 根本的な解決にならない

### 3. MSBuildの詳細ログでカバレッジ設定を確認 ❌
**実施内容:**
- 診断レベルのログでテスト実行
- カバレッジ設定の詳細確認

**結果:**
- テストは実際にInfrastructureとPresentationのコードを実行
- ログからInfrastructureサービスの実行を確認
- カバレッジファイルには`BrowserSelector.App`のみが含まれる

**問題点:**
- 設定と実際の動作に乖離
- カバレッジ測定の根本的な問題

### 4. 設定ファイルの見直し ❌
**実施内容:**
- `coverage.runsettings`ファイルの作成
- より詳細なInclude/Exclude設定

**結果:**
- 設定ファイルは正しく作成される
- しかし、コマンド実行で問題が発生

**問題点:**
- 設定ファイルの読み込みに問題
- コマンド実行の不安定性

### 5. プロジェクトファイルの直接修正 ❌
**実施内容:**
- Includeパターンを`[*]*`に変更
- より包括的な設定

**結果:**
- カバレッジファイルは生成される
- しかし、`BrowserSelector.App`のみが含まれる

**問題点:**
- 設定変更でも同じ問題が発生
- 根本的な解決にならない

## 根本原因の分析

### 技術的な問題
1. **.NET 8.0とcoverletの互換性問題**
   - 設定は正しく見えるが、実際の測定結果に反映されない
   - プロジェクト参照の問題

2. **カバレッジ測定の根本的な制限**
   - 特定のプロジェクトのみが測定対象になる
   - Include/Excludeパターンが期待通りに動作しない

3. **プロジェクトビルド順序の問題**
   - カバレッジ測定時のプロジェクト参照順序
   - 依存関係の問題

### 確認された事実
- ✅ テストは実際にCoreとInfrastructureのコードを実行している
- ✅ ログからInfrastructureサービスの実行を確認
- ❌ カバレッジファイルには`BrowserSelector.App`のみが含まれる
- ❌ すべての代替アプローチで同じ問題が発生

## 推奨される解決策

### 1. ツールの更新・変更
**推奨アクション:**
- **coverletの最新版への更新**: 互換性の改善
- **dotnet-coverageの代替ツール**: より新しいカバレッジ測定ツール
- **Visual Studio Code Coverage**: IDE統合ツールの使用

### 2. アーキテクチャの見直し
**推奨アクション:**
- **プロジェクト構造の簡素化**: カバレッジ測定の複雑さを軽減
- **依存関係の最適化**: プロジェクト参照の整理
- **テスト戦略の見直し**: カバレッジ測定に適した構造

### 3. 段階的なアプローチ
**推奨アクション:**
- **プロジェクト別の個別測定**: 各プロジェクトを個別に測定
- **手動でのカバレッジ統合**: 複数のカバレッジファイルを手動で統合
- **CI/CDパイプラインでの測定**: 自動化された環境での測定

## 結論

### 実施結果
- ✅ 5つの代替アプローチを検討・実装
- ✅ 各アプローチでカバレッジファイルは生成される
- ❌ すべてのアプローチで同じ問題が発生
- ❌ 根本的な解決には至らない

### 根本原因
.NET 8.0とcoverletの設定互換性の問題、またはプロジェクトビルド順序の問題が根本原因である可能性が高い。

### 推奨アクション
1. **短期**: プロジェクト別の個別測定と手動統合
2. **中期**: ツールの更新・変更
3. **長期**: アーキテクチャの見直し

### 次のステップ
1. プロジェクト別の個別測定の実装
2. 手動でのカバレッジ統合の検討
3. より新しいカバレッジ測定ツールの調査

## 技術的詳細

### 生成されたカバレッジファイル
- **TestResults/Core/**: Coreプロジェクトテスト用
- **TestResults/Detailed/**: 詳細ログ用
- **TestResults/UnitTests/**: 通常のUnitTests用
- **TestResults/Wildcard/**: Wildcard設定用
- **TestResults/Alternative/**: 代替設定用

### すべてのファイルで同じ結果
- カバレッジ率: 0%
- 含まれるプロジェクト: `BrowserSelector.App`のみ
- カバー可能行数: 247行
- カバー行数: 0行

この結果は、カバレッジ測定の根本的な問題を示しており、設定の変更だけでは解決できないことを示しています。
